// Formatting is mechanical and must never be a review topic
// (27_QA_TESTING_AND_UAT_PLAN.md §3 lists formatting as a mandatory static check).
/** @type {import("prettier").Config} */
export default {
  printWidth: 110,
  singleQuote: false,
  semi: true,
  trailingComma: "all",
  arrowParens: "always",
  endOfLine: "lf",
};
